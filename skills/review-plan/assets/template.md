# Plan レビュー結果

> Issue: #123 https://github.com/owner/repo/issues/123
> Plan: tmp/issues/123/plan.md

## サマリ

- 修正必須: N 件
- 任意改善: N 件
- 判定: **差し戻し必要 / OK**

## 受け入れ条件カバレッジ

| AC | 内容 | カバー状況 |
|---|---|---|
| AC1 | 不正なメールアドレスを入力した場合にエラーメッセージが表示される | OK (タスク 1, 2) |
| AC2 | 正常なメールアドレスを入力した場合にエラーが発生しない | OK (タスク 1) |
| AC3 | メールアドレス未入力時に必須項目エラーが表示される | **未カバー** |

## 修正必須 (must)

### 1. AC3（必須項目エラー表示）が plan のタスクでカバーされていない

- 該当箇所: plan.md 受け入れ条件カバレッジ
- 検証根拠: AC3 の対応タスク欄が空
- 提案: タスク 1 のテストケースに「メールアドレス未入力時に必須エラーが返る」を追加するか、新規タスクを起こす

### 2. orders/* への書き込みに伴う通知文面の更新が plan に含まれていない

- 該当箇所: plan.md タスク 2「注文確定処理の追加」
- 検証根拠:
  - `attentions` の「orders/* への書き込みは Discord 通知を発火」に該当
  - `grep -r "orders/" functions/src/` で `functions/src/triggers/onOrderWrite.ts` がヒット → 通知文面を組み立てている
- 提案: plan のタスク 2 の影響範囲に `functions/src/triggers/onOrderWrite.ts` の通知文面確認を追加

## 任意改善 (should)

### 1. テストケースに空配列のエッジケースが含まれていない

- 該当箇所: plan.md タスク 1「バリデーション追加」
- 提案: 空配列入力時の挙動を Red テストに追加すると安全

## OK

- 直接参照の影響範囲は適切にカバーされている
- 暗黙の必須セット（ユーザー作成 = Auth + Firestore）も plan に反映済み
